#Index Positions starts at 0, Not 1.
Today's test on p74 got 100 points!
chaging,adding and removing elements.
#为什么不可以用.append()给veriables重新负值？
#How to decide whether to use del statement or the pop() method:
 When you want to delete anitem from a list and not use that item 
 in any way, use the del statement; if you want to use an item 
 as you remove it, use the pop() method.
Today we are learning 'organizing a list.'
sort()methord, sorted()function 这个阶段work for lowercase并够用, 其他情况会比较复杂。
reverse()methord
finding the length of a list