# + 号 前后 要类型一样， 变 量 和 字符串拼接， 变量也许要是字符串类型的。
3 types whitespace 1. " "  2. \n   3.\t 
list 增删改查
3🀄修改方式：
    - del motorcycles   statement 声明式的修改
    - motocycles.pop()   method 方法式的修改
    - sorted(motorcycles) function 函数式的修改

range(,) 创建一个list，  list[:] 在list里面切分区块。 都是包左不包右原则。不写数字，就顶头。
我们学了
    - Looping through an entire list
    - Making numerical list
    - Slicing a list
    - Looping through a slice
    - Copying a list 用slice的方式

已学到的数据类型种类
    - 第一种数据类型是变量：strings, numericals, Boolean. 
      Boolean 是第三种veriable. 它只有ture 和false两种值。
    - 第二种数据类型是list/tuple
    - 第三种数据类型是dictionary

第五章 if statement

各种格式：

第一个种是if格式:
if conditional_test:
    do something

第二种格式if else statement
if conditional_test:
    do something
else:
    do something

第三种格式 if-elif-else statement
if conditional_test:
    do something
elif other_conditional_test:
    do something 
else:
    do something
# elif 可以有好几个，else可有可无
  并且任何一个条件满足，就会跳过其他格式
  结合遍历可以判断list的各个element

# 检查一个list 是否为空的专有写法, e.g.
if list_name:
    print("This list is not empty.")
else:
    print("This list is empty.")

Chaper 6
#遍历dictionary的标准公式
for key, value in user_0.items()
    print()print("\nkey:" + key)
    print("value:" + value)

如果只遍历打印keys，就写：
for key in user_0.keys()
    print()print("\nkey:" + key)

也可以只遍历打印values

Chapter 7
# The while loop runs as long as the list is not empty. 要等list里面没东西 才会跳出来。
# A for loop is effective for looping through a list, you shouldn't modify a list inside a for loop because Python will have trouble keeping track of the items in the list.
